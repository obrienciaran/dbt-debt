"""dbt-debt: a technical-debt scorecard for dbt projects.

Supports BigQuery, Snowflake, Redshift, and Databricks. Keep this list, the `description` in
`pyproject.toml`, and the parser description in `cli.py` in step when a warehouse is added.
"""

__version__ = "0.0.1"
